{
  "name": "attestation-wasm-dev",
  "private": true,
  "description": "Developer scripts for the attestation wasm build; the published package is what wasm-pack writes to pkg/",
  "type": "module",
  "engines": {
    "node": ">=21"
  },
  "scripts": {
    "build": "wasm-pack build --target nodejs --release",
    "conformance": "node conformance.mjs"
  }
}
